{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h2>Registre um novo paciente:</h2>
<div class="card">
    <div class="card-body">
       <form action="/crud/pacientes/new" method="post">
    <label for="nome">Nome:</label>
        <input type="text" id="nome" name="nome" placeholder="Informe o nome do paciente" class="form-control">
    <label for="endereço">Endereço:</label>
        <input type="text" name="endereco" id="endereco" placeholder="Informe o endereço" class="form-control">
    <label for="cartao">Cartão de saúde: </label>
        <input type="number" name="cartao" id="cartao" placeholder="Informe o ID do seu catrão de saúde. 9 digitos exatos" class="form-control">
    <button type="submit" class="btn btn-success mt-4">Registrar paciente</button>
</form> 
    </div>
</div>
